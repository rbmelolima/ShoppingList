import 'package:flutter/material.dart';

class UpdateListPage extends StatelessWidget {
  const UpdateListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
